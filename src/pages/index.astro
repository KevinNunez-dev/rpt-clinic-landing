---
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>RPT Clinic - Robotic Precision Therapy</title>
    <meta name="description" content="Experience pain relief without medication, needles, or surgery. Discover robotic precision therapy at RPT Clinic." />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        color: #1a1a1a;
        line-height: 1.6;
        background: #0f0f1e;
      }

      /* Animated background gradient */
      body::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, #0f0f1e 0%, #1a1a3e 25%, #2d1b4e 50%, #1a1a3e 75%, #0f0f1e 100%);
        background-size: 400% 400%;
        animation: gradientBg 15s ease infinite;
        z-index: -1;
        opacity: 0.5;
      }

      @keyframes gradientBg {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }

      /* Header/Navigation */
      header {
        background: rgba(15, 15, 30, 0.8);
        backdrop-filter: blur(20px);
        border-bottom: 1px solid rgba(102, 126, 234, 0.2);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        position: sticky;
        top: 0;
        z-index: 1000;
      }

      nav {
        max-width: 1400px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1.2rem 2rem;
      }

      .logo img {
        height: 50px;
        width: auto;
        filter: drop-shadow(0 0 10px rgba(102, 126, 234, 0.3));
      }

      nav ul {
        display: flex;
        list-style: none;
        gap: 3rem;
      }

      nav a {
        text-decoration: none;
        color: #e8e8e8;
        transition: all 0.3s ease;
        font-weight: 500;
        position: relative;
        padding: 0.5rem 0;
      }

      nav a::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, #40c557 0%, #228b22 100%);
        transition: width 0.3s ease;
      }

      nav a:hover {
        color: #40c557;
      }

      nav a:hover::after {
        width: 100%;
      }

      /* Hero Section */
      .hero {
        position: relative;
        padding: 120px 2rem;
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: center;
        overflow: hidden;
      }

      .hero::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -10%;
        width: 500px;
        height: 500px;
        background: radial-gradient(circle, rgba(64, 197, 87, 0.15) 0%, transparent 70%);
        border-radius: 50%;
        z-index: 0;
      }

      .hero::after {
        content: '';
        position: absolute;
        bottom: -50%;
        left: -10%;
        width: 500px;
        height: 500px;
        background: radial-gradient(circle, rgba(34, 139, 34, 0.15) 0%, transparent 70%);
        border-radius: 50%;
        z-index: 0;
      }

      .hero-content {
        position: relative;
        z-index: 1;
      }

      .hero-content h1 {
        font-size: clamp(2.5rem, 5vw, 4rem);
        margin-bottom: 1.5rem;
        background: linear-gradient(135deg, #ffffff 0%, #40c557 50%, #228b22 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1.2;
        font-weight: 800;
      }

      .hero-content p {
        font-size: 1.2rem;
        color: #b8b8b8;
        margin-bottom: 2rem;
        line-height: 1.8;
      }

      .rating {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-bottom: 2rem;
        color: #40c557;
      }

      .hero-image {
        position: relative;
        z-index: 1;
      }

      .hero-image img {
        width: 100%;
        height: auto;
        border-radius: 20px;
        box-shadow: 0 25px 50px rgba(64, 197, 87, 0.3);
        transition: all 0.4s ease;
      }

      .hero-image img:hover {
        transform: translateY(-10px);
        box-shadow: 0 35px 70px rgba(64, 197, 87, 0.5);
      }

      .cta-button {
        display: inline-block;
        background: linear-gradient(135deg, #40c557 0%, #228b22 100%);
        color: white;
        padding: 16px 48px;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 700;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: 0 8px 24px rgba(64, 197, 87, 0.4);
        border: 2px solid transparent;
        position: relative;
        overflow: hidden;
      }

      .cta-button::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
        transition: left 0.5s;
      }

      .cta-button:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 40px rgba(64, 197, 87, 0.6);
      }

      .cta-button:hover::before {
        left: 100%;
      }

      /* Three Benefits Section - TRANSPARENT */
      .benefits {
        padding: 100px 2rem;
        position: relative;
        max-width: 1400px;
        margin: 0 auto;
        background: transparent;
        border-radius: 0;
        margin-top: 60px;
        margin-bottom: 60px;
      }

      .benefits-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 2.5rem;
      }

      .benefit-card {
        background: rgba(255, 255, 255, 0.06);
        backdrop-filter: blur(20px);
        padding: 3rem 2rem;
        border-radius: 20px;
        text-align: center;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
        border: 2px solid rgba(64, 197, 87, 0.3);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      }

      .benefit-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(64, 197, 87, 0.2), transparent);
        transition: left 0.5s;
        z-index: 0;
      }

      .benefit-card:hover {
        transform: translateY(-15px);
        border-color: rgba(64, 197, 87, 0.6);
        box-shadow: 0 20px 60px rgba(64, 197, 87, 0.4);
        background: rgba(255, 255, 255, 0.1);
      }

      .benefit-card h3 {
        font-size: 1.5rem;
        margin: 1rem 0;
        background: linear-gradient(135deg, #40c557 0%, #228b22 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        position: relative;
        z-index: 1;
      }

      .benefit-card p {
        color: #b8b8b8;
        position: relative;
        z-index: 1;
      }

      /* Service Section */
      .service-section {
        padding: 120px 2rem;
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: center;
      }

      .service-section h2 {
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        margin-bottom: 1.5rem;
        background: linear-gradient(135deg, #ffffff 0%, #40c557 50%, #228b22 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 800;
      }

      .service-section p {
        font-size: 1.1rem;
        color: #b8b8b8;
        margin-bottom: 2rem;
        line-height: 1.8;
      }

      .service-image img {
        width: 100%;
        height: auto;
        border-radius: 20px;
        box-shadow: 0 25px 50px rgba(64, 197, 87, 0.3);
        transition: all 0.4s ease;
      }

      .service-image img:hover {
        transform: scale(1.05);
        box-shadow: 0 35px 70px rgba(64, 197, 87, 0.5);
      }

      .features-list {
        list-style: none;
        margin: 2rem 0;
      }

      .features-list li {
        padding: 1rem 0;
        color: #b8b8b8;
        display: flex;
        align-items: center;
        font-size: 1.05rem;
        transition: all 0.3s ease;
      }

      .features-list li:hover {
        color: #40c557;
        transform: translateX(10px);
      }

      .features-list li::before {
        content: '✓';
        color: #40c557;
        font-weight: bold;
        margin-right: 1rem;
        font-size: 1.5rem;
        background: linear-gradient(135deg, #40c557 0%, #228b22 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Testimonials Section - GREEN GRADIENT */
      .testimonials {
        background: linear-gradient(135deg, #228b22 0%, #40c557 100%);
        padding: 100px 2rem;
        position: relative;
        overflow: hidden;
      }

      .testimonials::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -10%;
        width: 500px;
        height: 500px;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
        border-radius: 50%;
      }

      .testimonials h2 {
        text-align: center;
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        margin-bottom: 4rem;
        color: white;
        font-weight: 800;
        position: relative;
        z-index: 1;
      }

      .testimonials-grid {
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 2.5rem;
        position: relative;
        z-index: 1;
      }

      .testimonial-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(20px);
        border-radius: 20px;
        padding: 2.5rem;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.3);
      }

      .testimonial-card::before {
        content: '"';
        position: absolute;
        top: -30px;
        left: 15px;
        font-size: 150px;
        color: rgba(255, 255, 255, 0.1);
        font-family: Georgia, serif;
        line-height: 1;
        z-index: 0;
      }

      .testimonial-card:hover {
        transform: translateY(-15px);
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        background: rgba(255, 255, 255, 1);
      }

      .testimonial-content {
        position: relative;
        z-index: 1;
      }

      .testimonial-card .rating {
        color: #ffc107;
        font-size: 1.3rem;
        margin-bottom: 1.5rem;
        display: flex;
        gap: 4px;
      }

      .testimonial-card p {
        font-size: 1.05rem;
        line-height: 1.8;
        color: #333;
        margin-bottom: 1.5rem;
        font-style: italic;
      }

      .testimonial-author {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding-top: 1.5rem;
        border-top: 2px solid rgba(64, 197, 87, 0.3);
      }

      .author-avatar {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: linear-gradient(135deg, #40c557 0%, #228b22 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: bold;
        font-size: 1.2rem;
        box-shadow: 0 4px 15px rgba(64, 197, 87, 0.4);
      }

      .author-info strong {
        display: block;
        color: #222;
        font-size: 1rem;
        margin-bottom: 0.2rem;
      }

      .author-info span {
        color: #999;
        font-size: 0.9rem;
      }

      .testimonial-card:nth-child(1) {
        animation: slideInUp 0.6s ease 0.1s backwards;
      }

      .testimonial-card:nth-child(2) {
        animation: slideInUp 0.6s ease 0.2s backwards;
      }

      .testimonial-card:nth-child(3) {
        animation: slideInUp 0.6s ease 0.3s backwards;
      }

      /* FAQ Section - TRANSPARENT */
      .faq {
        padding: 100px 2rem;
        max-width: 1400px;
        margin: 0 auto;
        background: transparent;
        border-radius: 0;
        margin-top: 60px;
        margin-bottom: 60px;
      }

      .faq h2 {
        text-align: center;
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        margin-bottom: 4rem;
        background: linear-gradient(135deg, #228b22 0%, #40c557 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 800;
      }

      .faq-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 2rem;
      }

      .faq-item {
        background: rgba(255, 255, 255, 0.06);
        backdrop-filter: blur(20px);
        padding: 2rem;
        border-radius: 16px;
        cursor: pointer;
        transition: all 0.3s;
        border: 2px solid rgba(64, 197, 87, 0.3);
        position: relative;
        overflow: hidden;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      }

      .faq-item::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 3px;
        background: linear-gradient(90deg, #40c557 0%, #228b22 100%);
        transition: left 0.3s;
      }

      .faq-item:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: rgba(64, 197, 87, 0.6);
        transform: translateY(-5px);
        box-shadow: 0 12px 40px rgba(64, 197, 87, 0.3);
      }

      .faq-item:hover::before {
        left: 0;
      }

      .faq-item h4 {
        color: #e8e8e8;
        margin-bottom: 0.5rem;
        font-size: 1.1rem;
      }

      .faq-item p {
        color: #b8b8b8;
        font-size: 0.95rem;
        display: none;
        line-height: 1.6;
      }

      .faq-item.active p {
        display: block;
        animation: fadeIn 0.4s ease;
      }

      /* Team Section */
      .team {
        padding: 100px 2rem;
        max-width: 1400px;
        margin: 0 auto;
        position: relative;
      }

      .team h2 {
        text-align: center;
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        margin-bottom: 4rem;
        background: linear-gradient(135deg, #ffffff 0%, #40c557 50%, #228b22 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 800;
      }

      .team-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 2.5rem;
      }

      .team-card {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(20px);
        border-radius: 20px;
        overflow: hidden;
        text-align: center;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        border: 1px solid rgba(64, 197, 87, 0.2);
        position: relative;
      }

      .team-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 4px;
        background: linear-gradient(90deg, #40c557 0%, #228b22 100%);
        transform: scaleX(0);
        transition: transform 0.4s;
        z-index: 1;
      }

      .team-card:hover::before {
        transform: scaleX(1);
      }

      .team-card:hover {
        transform: translateY(-20px);
        border-color: rgba(64, 197, 87, 0.5);
        box-shadow: 0 25px 60px rgba(64, 197, 87, 0.3);
      }

      .team-image {
        width: 100%;
        height: 300px;
        background: linear-gradient(135deg, rgba(64, 197, 87, 0.1) 0%, rgba(34, 139, 34, 0.1) 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #999;
        overflow: hidden;
        position: relative;
      }

      .team-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.4s ease;
      }

      .team-card:hover .team-image img {
        transform: scale(1.1);
      }

      .team-info {
        padding: 2rem;
      }

      .team-info h3 {
        color: #e8e8e8;
        margin-bottom: 0.5rem;
        font-weight: 700;
      }

      .team-info p {
        color: #999;
        font-size: 0.9rem;
        line-height: 1.6;
      }

      /* Contact/CTA Section */
      .contact-section {
        background: linear-gradient(135deg, #40c557 0%, #228b22 100%);
        position: relative;
        padding: 100px 2rem;
        overflow: hidden;
      }

      .contact-section::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -20%;
        width: 600px;
        height: 600px;
        background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
        border-radius: 50%;
      }

      .contact-content {
        max-width: 600px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
      }

      .contact-section h2 {
        font-size: 2.5rem;
        margin-bottom: 2rem;
        text-align: center;
        color: white;
      }

      .form-group {
        margin-bottom: 1.5rem;
      }

      .form-group label {
        display: block;
        margin-bottom: 0.8rem;
        font-weight: 600;
        color: rgba(255, 255, 255, 0.9);
      }

      .form-group input,
      .form-group textarea {
        width: 100%;
        padding: 14px 16px;
        border: 2px solid rgba(255, 255, 255, 0.2);
        border-radius: 12px;
        font-family: inherit;
        font-size: 1rem;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        color: white;
        transition: all 0.3s ease;
      }

      .form-group input::placeholder,
      .form-group textarea::placeholder {
        color: rgba(255, 255, 255, 0.6);
      }

      .form-group input:focus,
      .form-group textarea:focus {
        outline: none;
        border-color: rgba(255, 255, 255, 0.5);
        background: rgba(255, 255, 255, 0.15);
        box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
      }

      .form-group textarea {
        resize: vertical;
        min-height: 120px;
      }

      .submit-button {
        width: 100%;
        background: white;
        color: #228b22;
        border: none;
        padding: 16px;
        border-radius: 12px;
        font-weight: 700;
        font-size: 1.05rem;
        cursor: pointer;
        transition: all 0.3s;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
      }

      .submit-button:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 32px rgba(0, 0, 0, 0.3);
        background: rgba(255, 255, 255, 0.95);
      }

      /* Footer */
      footer {
        background: rgba(15, 15, 30, 0.8);
        backdrop-filter: blur(20px);
        border-top: 1px solid rgba(64, 197, 87, 0.2);
        color: #999;
        padding: 2rem;
        text-align: center;
      }

      /* Location Section */
      .location {
        padding: 100px 2rem;
        max-width: 1400px;
        margin: 0 auto;
        text-align: center;
      }

      .location h2 {
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        margin-bottom: 3rem;
        background: linear-gradient(135deg, #ffffff 0%, #40c557 50%, #228b22 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 800;
      }

      .location-info {
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(20px);
        padding: 3rem 2rem;
        border-radius: 16px;
        margin-bottom: 3rem;
        border: 1px solid rgba(64, 197, 87, 0.2);
      }

      .location-info h3 {
        color: #e8e8e8;
        margin-bottom: 1rem;
      }

      .location-info p {
        color: #b8b8b8;
        margin: 0.5rem 0;
      }

      .map {
        width: 100%;
        height: 400px;
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 20px 50px rgba(64, 197, 87, 0.3);
        border: 1px solid rgba(64, 197, 87, 0.2);
      }

      /* Animations */
      @keyframes slideInUp {
        from {
          opacity: 0;
          transform: translateY(40px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
      }

      /* Responsive */
      @media (max-width: 968px) {
        .hero,
        .service-section {
          grid-template-columns: 1fr;
          gap: 2rem;
        }

        nav ul {
          gap: 1.5rem;
          font-size: 0.9rem;
        }

        .testimonials-grid,
        .team-grid,
        .benefits-grid {
          grid-template-columns: 1fr;
        }

        .hero-content h1,
        .service-section h2,
        .faq h2,
        .team h2,
        .location h2 {
          font-size: 2rem;
        }

        .benefits,
        .faq {
          margin-left: 1rem;
          margin-right: 1rem;
          padding: 60px 2rem;
        }
      }

      @media (max-width: 600px) {
        nav {
          flex-direction: column;
          gap: 1rem;
        }

        nav ul {
          flex-wrap: wrap;
          gap: 1rem;
          justify-content: center;
        }

        .hero {
          padding: 60px 1rem;
        }

        .contact-section {
          padding: 60px 1rem;
        }
      }
    </style>
  </head>
  <body>
    <!-- Header -->
    <header>
      <nav>
        <div class="logo">
          <img src="public/Logo.png" alt="RPT Clinic Logo" />
        </div>
        <ul>
          <li><a href="#services">Services</a></li>
          <li><a href="#testimonials">Testimonials</a></li>
          <li><a href="#faq">FAQ</a></li>
          <li><a href="#team">Team</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <h1>Keyword Relief</h1>
        <p>Experience pain-free living with advanced robotic precision therapy in just 4-6 weekly sessions.</p>
        <div class="rating">★★★★★ Trusted by hundreds of patients</div>
        <a href="#contact" class="cta-button">Book Your Free Consultation</a>
      </div>
      <div class="hero-image">
        <img src="public/robot-hero.jpg" alt="Robotic Precision Therapy Robot" />
      </div>
    </section>

    <!-- Three Benefits -->
    <section class="benefits">
      <div class="benefits-grid">
        <div class="benefit-card">
          <h3>No Meds</h3>
          <p>Zero medications required. Experience pure robotic healing technology without pills or prescriptions.</p>
        </div>
        <div class="benefit-card">
          <h3>No Needles</h3>
          <p>Completely non-invasive treatment. No injections, no pain, just precise robotic therapy.</p>
        </div>
        <div class="benefit-card">
          <h3>No Surgery</h3>
          <p>Surgical-free solution for lasting pain relief without incisions or lengthy recovery times.</p>
        </div>
      </div>
    </section>

    <!-- Service Section -->
    <section class="service-section" id="services">
      <div>
        <h2>Robotic Precision Therapy</h2>
        <p>The Easiest, Most Comfortable Way To Resolve Your Pain</p>
        <ul class="features-list">
          <li>Advanced robotic technology</li>
          <li>Personalized treatment plans</li>
          <li>Clinically proven results</li>
          <li>Quick recovery time</li>
          <li>Comfortable sessions</li>
        </ul>
      </div>
      <div class="service-image">
        <img src="public/therapy-machine.jpg" alt="Robotic Therapy Machine" />
      </div>
    </section>

    <!-- Testimonials -->
    <section class="testimonials" id="testimonials">
      <h2>Inspiring Testimonials: From Pain to Freedom</h2>
      <div class="testimonials-grid">
        
        <div class="testimonial-card">
          <div class="testimonial-content">
            <div class="rating">★★★★★</div>
            <p>"After years of chronic back pain, RPT Clinic gave me my life back. The treatment was comfortable, non-invasive, and incredibly effective. I can now play with my grandchildren pain-free!"</p>
            <div class="testimonial-author">
              <div class="author-avatar">JD</div>
              <div class="author-info">
                <strong>Jennifer Davis</strong>
                <span>Verified Patient</span>
              </div>
            </div>
          </div>
        </div>

        <div class="testimonial-card">
          <div class="testimonial-content">
            <div class="rating">★★★★★</div>
            <p>"I was skeptical at first, but the results speak for themselves. No medications, no surgery, just pure relief. The staff was professional and caring throughout my entire treatment journey."</p>
            <div class="testimonial-author">
              <div class="author-avatar">MR</div>
              <div class="author-info">
                <strong>Michael Rodriguez</strong>
                <span>Verified Patient</span>
              </div>
            </div>
          </div>
        </div>

        <div class="testimonial-card">
          <div class="testimonial-content">
            <div class="rating">★★★★★</div>
            <p>"Best decision I ever made for my health. The robotic precision therapy is truly revolutionary. I've recommended RPT Clinic to all my friends and family dealing with pain."</p>
            <div class="testimonial-author">
              <div class="author-avatar">SK</div>
              <div class="author-info">
                <strong>Sarah Kim</strong>
                <span>Verified Patient</span>
              </div>
            </div>
          </div>
        </div>

      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq" id="faq">
      <h2>Common Questions</h2>
      <div class="faq-grid">
        <div class="faq-item" onclick="this.classList.toggle('active')">
          <h4>✨ Does RPT work?</h4>
          <p>Yes, RPT has been clinically proven effective with thousands of satisfied patients experiencing significant pain relief and improved quality of life.</p>
        </div>
        <div class="faq-item" onclick="this.classList.toggle('active')">
          <h4>⏱️ How many sessions do I need?</h4>
          <p>Most patients see results within 4-6 weekly sessions, though some may need additional treatments based on their specific condition and severity.</p>
        </div>
        <div class="faq-item" onclick="this.classList.toggle('active')">
          <h4>😌 Is it painful?</h4>
          <p>No, RPT is completely non-invasive and comfortable. Patients often describe sessions as relaxing and rejuvenating.</p>
        </div>
        <div class="faq-item" onclick="this.classList.toggle('active')">
          <h4>💳 Do insurance plans cover RPT?</h4>
          <p>Many insurance plans do cover RPT. Please contact us for specific information about your coverage and benefits.</p>
        </div>
      </div>
    </section>

    <!-- Team Section -->
    <section class="team" id="team">
      <h2>Meet Our Specialists</h2>
      <div class="team-grid">
        
        <div class="team-card">
          <div class="team-image">
            <img src="public/Dayne.jpg" alt="Dr. Dayne" />
          </div>
          <div class="team-info">
            <h3>Dr. Dayne</h3>
            <p>Lead RPT Specialist<br>15+ years experience</p>
          </div>
        </div>

        <div class="team-card">
          <div class="team-image">
            <img src="public/Stephanie.jpg" alt="Stephanie Burgess" />
          </div>
          <div class="team-info">
            <h3>Stephanie Burgess</h3>
            <p>Therapy Specialist<br>10+ years experience</p>
          </div>
        </div>

        <div class="team-card">
          <div class="team-image">
            <img src="public/Mike.jpg" alt="Mike Rosenbergh" />
          </div>
          <div class="team-info">
            <h3>Mike Rosenbergh</h3>
            <p>Clinical Coordinator<br>8+ years experience</p>
          </div>
        </div>

        <div class="team-card">
          <div class="team-image">
            <img src="public/Monica.jpg" alt="Monica Simons" />
          </div>
          <div class="team-info">
            <h3>Monica Simons</h3>
            <p>Patient Care Coordinator<br>7+ years experience</p>
          </div>
        </div>

      </div>
    </section>

    <!-- Location -->
    <section class="location">
      <h2>Our Location</h2>
      <div class="location-info">
        <h3>RPT Clinic - Troy, Michigan</h3>
        <p>📍 123 Medical Center Rd, Troy, MI 48098</p>
        <p>📞 (248) 555-0123</p>
        <p>📧 info@rptclinic.com</p>
      </div>
      <div class="map">
        <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2958.1234567890!2d-83.2843!3d42.5801!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x8824a5d5d5d5d5d5%3A0x1234567890abcdef!2sTroy%2C%20MI!5e0!3m2!1sen!2sus!4v1234567890" width="100%" height="100%" style="border:0;" allowfullscreen="" loading="lazy"></iframe>
      </div>
    </section>

    <!-- Contact/CTA -->
    <section class="contact-section" id="contact">
      <div class="contact-content">
        <h2>Get Started With A Free Consultation</h2>
        <form onsubmit="handleSubmit(event)">
          <div class="form-group">
            <label for="name">Full Name</label>
            <input type="text" id="name" name="name" placeholder="Your name" required>
          </div>
          <div class="form-group">
            <label for="email">Email Address</label>
            <input type="email" id="email" name="email" placeholder="your@email.com" required>
          </div>
          <div class="form-group">
            <label for="phone">Phone Number</label>
            <input type="tel" id="phone" name="phone" placeholder="(248) 555-0123" required>
          </div>
          <div class="form-group">
            <label for="message">Tell us about your condition</label>
            <textarea id="message" name="message" placeholder="Describe your pain or condition..." required></textarea>
          </div>
          <button type="submit" class="submit-button">Schedule Free Consultation</button>
        </form>
      </div>
    </section>

    <!-- Footer -->
    <footer>
      <p>&copy; 2025 RPT Clinic. All rights reserved. | Privacy Policy | Terms of Service</p>
    </footer>

    <script>
      function handleSubmit(event) {
        event.preventDefault();
        alert('Thank you for your interest! We will contact you soon.');
        event.target.reset();
      }
    </script>
  </body>
</html>
