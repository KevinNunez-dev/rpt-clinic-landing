---
---

<section class="hero">
  <div class="hero-content">
    <h1>Chronic Back Pain Relief</h1>
    <p>Suffering from sciatica, herniated discs, or piriformis syndrome? Our revolutionary robotic precision therapy targets deep muscular imbalances that traditional treatments can't reach. FDA-registered, non-surgical relief in just 4-6 sessions.</p>
    <div class="rating">★★★★★ Trusted by 500+ Michigan patients</div>
    <a href="#contact" class="cta-button">Book Your Free Consultation</a>
  </div>
  <div class="hero-image">
    <img src="/public/robot-hero.jpg" alt="RX 2600 Robotic Precision Therapy" />
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: 120px 2rem;
    max-width: 1400px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -10%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(64, 197, 87, 0.15) 0%, transparent 70%);
    border-radius: 50%;
    z-index: 0;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: -50%;
    left: -10%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(34, 139, 34, 0.15) 0%, transparent 70%);
    border-radius: 50%;
    z-index: 0;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: slideInLeft 1.2s ease 0.2s forwards;
  }

  .hero-content h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    margin-bottom: 1.5rem;
    background: linear-gradient(135deg, #ffffff 0%, #40c557 50%, #228b22 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1.2;
    font-weight: 800;
  }

  .hero-content p {
    font-size: 1.2rem;
    color: #b8b8b8;
    margin-bottom: 2rem;
    line-height: 1.8;
  }

  .rating {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 2rem;
    color: #40c557;
  }

  .hero-image {
    position: relative;
    z-index: 1;
    opacity: 0;
    animation: slideInRight 1.2s ease 0.4s forwards;
  }

  .hero-image img {
    width: 100%;
    height: auto;
    border-radius: 20px;
    box-shadow: 0 25px 50px rgba(64, 197, 87, 0.3);
    transition: all 0.4s ease;
  }

  .hero-image img:hover {
    transform: translateY(-10px) scale(1.02);
    box-shadow: 0 35px 70px rgba(64, 197, 87, 0.5);
  }

  .cta-button {
    display: inline-block;
    background: linear-gradient(135deg, #40c557 0%, #228b22 100%);
    color: white;
    padding: 16px 48px;
    border-radius: 50px;
    text-decoration: none;
    font-weight: 700;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 8px 24px rgba(64, 197, 87, 0.4);
    border: 2px solid transparent;
    position: relative;
    overflow: hidden;
  }

  .cta-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.5s;
  }

  .cta-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 40px rgba(64, 197, 87, 0.6);
  }

  .cta-button:hover::before {
    left: 100%;
  }

  @keyframes slideInLeft {
    from {
      opacity: 0;
      transform: translateX(-60px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(60px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @media (max-width: 968px) {
    .hero {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }

  @media (max-width: 600px) {
    .hero {
      padding: 60px 1rem;
    }
  }
</style>

<script>
  // SMOOTH HERO IMAGE PARALLAX
  let ticking = false;
  let lastScrollY = 0;

  document.addEventListener('scroll', () => {
    lastScrollY = window.scrollY;

    if (!ticking) {
      window.requestAnimationFrame(() => {
        // cast the result so TypeScript knows this is an HTMLImageElement
        const heroImage = document.querySelector<HTMLImageElement>('.hero-image img');
        if (heroImage) {
          heroImage.style.transform = `translateY(${lastScrollY * 0.4}px)`;
        }
        ticking = false;
      });
      ticking = true;
    }
  }, { passive: true });
</script>
